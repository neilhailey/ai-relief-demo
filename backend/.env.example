# Required
OPENAI_API_KEY=sk-...

# Optional — uses Depth Anything V2 via Replicate for better depth maps
# Without this, falls back to luminance-based approximation (still looks good)
REPLICATE_API_TOKEN=r8_...

# Optional — override allowed CORS origins (default: *)
# CORS_ORIGINS=https://your-frontend.vercel.app
